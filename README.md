# Galler

Static image gallery viewer built with SolidJS. Fetches and displays images from external URLs with support for multiple image providers (imgbox.com, imx.to).

Uses the corsproxy.io service to fetch external pages.

Usage: Pass target URL as `url` parameter
